# ERC-8004 AI Code Review Service Dependencies

# Core Web3 and Blockchain (CRITICAL - All imports fixed)
web3>=6.0.0
eth-account>=0.13.0
eth-utils>=2.3.0
py-solc-x>=2.0.0

# Environment management (CRITICAL - Fixed)
python-dotenv>=1.1.0

# FastAPI and Web Framework (CRITICAL - Fixed)
fastapi>=0.114.2  # Allow newer versions for enhanced features
uvicorn[standard]>=0.35.0
websockets>=12.0

# Pydantic (CRITICAL - Fixed)
pydantic>=2.11.0
pydantic-core>=2.33.0

# AI Client Libraries (UPDATED)
# xAI SDK for Grok models
xai-sdk>=1.1.0

# Anthropic Claude
anthropic>=0.7.0

# OpenAI (fallback only)
openai>=1.0.0

# Legacy AI orchestration (optional)
crewai>=0.165.1
langchain>=0.1.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0

# HTTP clients
httpx>=0.25.0

# Code Analysis Tools
ast-decompiler>=0.5.0
pylint>=3.0.0
bandit>=1.7.5
radon>=6.0.1
flake8>=6.0.0
mypy>=1.6.0

# Phala Cloud TEE SDK
dstack-sdk>=0.1.0

# Cryptography for A2A Protocol
cryptography>=41.0.0

# API and Web Framework (removed duplicates)
requests>=2.31.0

# Data Processing (compatible with max-engine)
pandas>=2.0.0
numpy>=1.18,<2.0  # Pin to avoid max-engine conflicts

# File Handling
gitpython>=3.1.40

# Utilities  
click>=8.1.0
rich>=13.0.0
colorama>=0.4.6
tabulate>=0.9.0
pyyaml>=6.0.1
toml>=0.10.2

# Development and Testing Tools
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0
coverage>=7.3.0 